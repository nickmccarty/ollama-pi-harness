# requirements.txt — runtime dependencies for the harness.
# Transitive deps are resolved by pip automatically.
#
# PyTorch — needs a CUDA-specific index URL. Install separately:
#   pip install torch==2.7.1 --extra-index-url https://download.pytorch.org/whl/cu118
# Or use requirements-torch.txt:
#   pip install -r requirements-torch.txt
#
# Fine-tuning deps (peft/trl/bitsandbytes/accelerate):
#   pip install -r requirements-finetune.txt
#
# Dev/test deps (pytest/jupyter):
#   pip install -r requirements-dev.txt

# ---------------------------------------------------------------------------
# Inference backends
# ---------------------------------------------------------------------------
ollama==0.6.1
openai==2.31.0

# ---------------------------------------------------------------------------
# Search
# ---------------------------------------------------------------------------
ddgs==9.13.0
requests==2.32.5
feedparser==6.0.12
youtube-transcript-api==1.0.3

# ---------------------------------------------------------------------------
# Document processing / OCR
# ---------------------------------------------------------------------------
markitdown @ git+https://github.com/microsoft/markitdown.git@8a9d8f15936b2068bcb39ccc8d3b317f93784d86#egg=markitdown&subdirectory=packages/markitdown
pymupdf==1.27.2.2
pdfminer.six==20250506
pillow==11.3.0
beautifulsoup4==4.13.5
html2text==2024.2.26
mammoth==1.10.0
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
magika==0.6.2

# ---------------------------------------------------------------------------
# Memory / storage
# ---------------------------------------------------------------------------
chromadb==1.5.7
SQLAlchemy==2.0.49
aiosqlite==0.22.1

# ---------------------------------------------------------------------------
# Embeddings / ML
# ---------------------------------------------------------------------------
sentence-transformers==5.3.0
scikit-learn==1.8.0
numpy==2.3.2
scipy==1.17.1
torch>=2.0.0                    # install via requirements-torch.txt for CUDA builds

# ---------------------------------------------------------------------------
# LLM / HuggingFace ecosystem
# ---------------------------------------------------------------------------
transformers==5.5.0
huggingface_hub==1.9.2
tokenizers==0.22.2
safetensors==0.7.0
datasets==4.8.4                  # used by hf_export.py and eval pipelines
tiktoken==0.12.0

# ---------------------------------------------------------------------------
# Web server
# ---------------------------------------------------------------------------
Flask==3.1.3
APScheduler==3.11.2
Werkzeug==3.1.8

# ---------------------------------------------------------------------------
# Data analysis / visualization
# ---------------------------------------------------------------------------
pandas==2.3.2
matplotlib==3.10.8
seaborn==0.13.2
networkx==3.6.1

# ---------------------------------------------------------------------------
# Knowledge graph / templates
# ---------------------------------------------------------------------------
Jinja2==3.1.6
PyYAML==6.0.3
pyvis==0.3.2

# ---------------------------------------------------------------------------
# Agent scaffolding
# ---------------------------------------------------------------------------
tinytroupe @ git+https://github.com/microsoft/TinyTroupe.git@a6244b358a1fe1c71bf751f7ba0f8dfa368ec5a4
playwright==1.58.0               # web automation (used in research skills)
pyarrow==23.0.1                  # fast columnar IO for datasets
tqdm==4.67.3

# ---------------------------------------------------------------------------
# Utilities
# ---------------------------------------------------------------------------
python-dotenv==1.1.1
rich==14.3.3
tenacity==9.1.4
pydantic==2.12.5
typer==0.24.1
httpx==0.28.1
aiohttp==3.13.5
psutil==7.2.2
